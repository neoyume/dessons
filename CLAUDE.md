# Dessons — contexte projet

## Ce que c'est

Nom du projet : **Dessons** (contraction de "dessin" et "sons"). Repo GitHub prévu : `neoyume/dessons`.

Outil web mono-fichier (`index.html`, HTML + CSS + JS vanilla, zéro dépendance, zéro build) qui convertit une **image, une vidéo ou un flux webcam** en fichier `.mid` multi-pistes. Usage cible : générer des loops mélodiques/rythmiques à importer dans Logic Pro à partir d'un visuel (photo, illustration, texture, vidéo, caméra).

Deux sorties possibles : export d'un fichier `.mid`, ou lecture temps réel via **Web MIDI** vers un port virtuel (IAC Driver sur macOS) pour entendre/enregistrer la boucle directement dans Logic sans passer par un fichier.

Vidéo et webcam sont ré-analysées ~15 fps (`liveLoop()`) : les notes se recalculent en continu, la boucle MIDI suit l'image qui bouge. Bouton « Figer » pour capturer une frame.

Interface **bilingue FR/EN** (bouton `FR/EN`, détection `navigator.language` au 1er lancement, mémorisée en `localStorage` sous `dessons.lang`).

Tourne 100% dans le navigateur, aucune donnée envoyée à un serveur (l'image, la vidéo et le flux caméra ne quittent pas la machine). Pensé pour être publié tel quel sur GitHub Pages.

## Pourquoi ces choix (pour comprendre le code avant d'y toucher)

- **Pas de framework, pas de bundler** : un seul fichier `index.html`, exprès. Le projet reste "ouvre et ça marche", copiable, forkable sans setup. Toute évolution doit si possible rester dans cette philosophie (ou alors le dire explicitement si on bascule vers un vrai build).
- **Encodage MIDI écrit à la main** (pas de lib type `midi-writer-js`) : format SMF type 1, multi-pistes, delta-times en variable-length quantity. Voir les fonctions `writeVarLen`, `buildMidi`. C'est volontairement bas niveau pour ne dépendre de rien et garder un contrôle total sur la structure du fichier.
- **Détection par contours/contraste, pas par pixel brut** : contrairement à la plupart des convertisseurs image→MIDI existants (qui prennent la couleur moyenne d'un bloc de pixels), celui-ci calcule un gradient de luminosité (différence entre pixels voisins) et ne garde comme "notes" que les zones où ce gradient dépasse un seuil réglable. Ça donne des résultats plus musicaux sur des images à formes nettes (dessins, silhouettes, textures graphiques) que sur des photos très douces/floues.
- **Séparation en calques → pistes/instruments** : par teinte (hue), par bande de luminosité, ou par couleurs choisies à la pipette (`sepMode='pick'`), chaque calque devient une piste MIDI distincte avec son propre canal, son patch General MIDI, et son nom. C'est la fonctionnalité différenciante du projet par rapport à l'existant. Boutons **M**/**S** (mute/solo) par calque : `layerAudible(i)` = `anySolo() ? L.solo : !L.mute` (solo prime sur mute, convention DAW). N'affecte QUE la lecture temps réel (scheduler des deux sinks) et l'affichage des points d'aperçu (opacité) ; **`buildMidi()` écrit toujours tous les calques** (le `.mid` est le document complet, le mixage se fait dans le DAW).
- **Pipette de calque** : `process()` calcule `pr/pg/pb` (RGB ajusté par le pipeline ci-dessous, `adjustPixel()`) en plus de `lum`/`hue`. En `sepMode='pick'`, `layerOf(px)` **isole** plutôt que partitionne : un pixel rejoint le calque pipetté le plus proche **seulement si** la distance RGB² est ≤ `pickTol²` (slider « Tolérance couleur », `#pickTol`, id `pickTolSq` dans le code) ; sinon `layerOf` renvoie `-1` et le pixel ne compte pour aucun calque (silence), au lieu de tomber dans un bucket « le plus proche » comme avant. Un calque dont la couleur n'a jamais été pipettée (`L.picked !== true`) est **exclu du test**, il ne peut jamais matcher par accident sur sa couleur placeholder — d'où le cas d'usage voulu : pipette le vert des feuilles, seul le vert joue, le rouge de la fraise reste muet. `if(nLayers===1) return 0` (raccourci hue/lum) ne s'applique **pas** en `pick` : avec 1 seul calque pipetté, l'isolation reste réelle (pas de retour à « tout »). UI : `pickLayer` (index armé, -1 = aucun) ; clic sur la pastille (devenue `<button>`, tirets tant que `!L.picked`) arme, clic sur `#preview` échantillonne (`adjCtx.getImageData` — on échantillonne ce qui est *affiché*, pas le pixel brut) et écrit `L.color`/`L.pickColor`/`L.picked=true`. La boucle de rendu (`for(x0..x1)`) fait `if(L<0) continue;` avant d'indexer `bestEdge[L]`. `L.pickColor`/`L.picked` survivent aux changements de `sepMode` ; `rebuildLayers()` les restaure en repassant sur `pick`. `#pickTolRow` (affiché/masqué dans `rebuildLayers()`) n'apparaît qu'en mode `pick`.
- **Pipeline de pré-traitement, ordre fixe** (inversion → lissage → courbe de tons → saturation, puis postérisation/seuillage au moment de l'assignation en calque) : `process()` lit les pixels bruts de `imgCanvas`, applique `invert` (`#invert`, `255-valeur`) puis, si `#blur`>0, `boxBlur()` (3 passes horizontale+verticale, fenêtre glissante O(w×h) donc indépendante du rayon — approxime un flou gaussien sans en payer le coût). Le but du flou est d'empêcher un pixel isolé bruité de déclencher une fausse note (voir `img-03-blur.png` dans les tests : passer `blur` à 5 sur une image bruitée fait chuter le nombre de notes parasites de moitié tout en révélant les vraies arêtes). Puis `adjustPixel()` (ex-`adjustRGB`) applique une **LUT 256 entrées** construite par `buildToneLUT(blackPt, whitePt, gamma)` — remplace l'ancien slider unique « Contraste » par 3 sliders (`#blackPt`, `#whitePt`, `#gamma`, gamma en `step="0.05"`, vrai float HTML, pas d'entier mis à l'échelle) ; les valeurs par défaut (21 / 234 / 1.00) reproduisent quasi exactement l'ancien contraste fixe à 120 %, pour la rétrocompatibilité visuelle. La saturation (`sat`) s'applique ensuite, inchangée. Résultat baké dans `adjCanvas`/`adjCtx` (un `putImageData` par `process()`) : `drawPreview()` dessine directement `adjCanvas` (fini le `pctx.filter='contrast(...)'` approximatif — l'aperçu montre maintenant exactement ce qui est analysé, flou et inversion inclus) et l'eyedropper y échantillonne aussi.
- **Seuil automatique (Otsu)** : bouton `#autoThresh` à côté de « Sensibilité ». `process()` mémorise `lastEdgeArr`/`lastMaxEdge` (le tableau de magnitude de gradient et son max, quel que soit le mode) ; au clic, `otsuThreshold()` (algo standard, maximise la variance inter-classes) tourne sur un histogramme 256 bins de ces magnitudes — **pas** sur la luminosité brute, parce que « Sensibilité » seuille un gradient relatif (`cutoff=(1-threshPct)*maxEdge`), pas un niveau de gris absolu. Le bin trouvé est reconverti en pourcentage (`threshPct = 1 - bin/255`) et écrit dans `#thresh` via un `dispatchEvent('input')` normal (aucun code dédié, réutilise le pipeline existant).
- ~~Postérisation (`#posterize`)~~ : contrôle retiré (feature supprimée sur demande explicite de l'utilisateur, "j'arrive toujours pas à comprendre son utilité réelle") — `layerOf()` en `sepMode='hue'` bucket directement sur `hue[px]` brut, sans quantification intermédiaire. Si le besoin refait surface (calques imprévisibles sur une image au hue bruité), l'ancienne implémentation quantifiait la teinte en N paliers avant le bucketing (`Math.floor(hue/hueStep)*hueStep`) — voir l'historique git si on veut la reprendre plutôt que la réinventer.
- **Méthode « Par contours (Sobel) »** (`sepMode='sobel'`, 4ᵉ option) : `sobelAt()` calcule un vrai gradient Sobel 3×3 (Gx/Gy, bords repliés) — plus lourd que le gradient simple `|Δx|+|Δy|` utilisé par `hue`/`lum`/`pick`, donc calculé **seulement** quand ce mode est actif (les autres modes restent inchangés au bit près). Comme un gradient n'a pas de « couleur » naturelle à répartir en calques, `layerOf()` bucket par **orientation** de contour (`atan2(gy,gx) mod π`, sur `nLayers` tranches) : chaque calque capture une famille de directions d'arête plutôt qu'une teinte — résultat en lignes/silhouettes, pas en zones pleines. Pas de downscale/spinner de chargement nécessaire : l'image d'analyse est déjà plafonnée à `MAXDIM=260px`, donc même Sobel + Otsu + flou tournent en quelques millisecondes.
- **Recadrage (zone d'intérêt)** : `cropRect` (`{x,y,w,h}` normalisé [0,1] sur le cadre source complet, `null` = image entière) est appliqué à la source dans `fitCanvas()` (dimensionne `imgCanvas` sur les proportions du crop, pas de l'image entière) et `grabFrame()` (`drawImage` 9-arguments pour n'extraire que ce sous-rectangle). Le reste du pipeline (`process()`, `adjCanvas`, l'export, le direction/curve, la pipette) ne sait même pas qu'un crop existe — il voit juste un `imgCanvas` plus petit, exactement comme avant cette fonctionnalité. Éditeur (`#cropBtn`/`#cropApplyBtn`/`#cropResetBtn`, panneau gauche) : glisser-déposer redessine `pendingCropRect` en entier (pas de poignées de redimensionnement — un nouveau drag remplace l'ancien, comme un outil de capture d'écran), pointer events sur `#preview` (`normPointFromEvent`/`rectFromPoints`), rendu par `drawCropEditor()` qui affiche le cadre **complet non recadré** assombri hors de la sélection (le point étant justement de voir ce qui sera exclu) — jamais `adjCanvas`, qui ne reflète que le crop déjà appliqué. `redrawPreview()` et la fin de `process()` basculent vers `drawCropEditor()` tant que `cropEditing` est vrai, pour qu'aucun autre contrôle (curseur, sens, courbe…) ne vienne écraser l'overlay pendant l'édition. `stopSource()` réinitialise toujours `cropRect`/`cropEditing` : changer de source repart d'un cadrage plein cadre.
- **Sensibilité par calque** : chaque `layers[i]` porte son propre `thresh` (1-100, mêmes unités que le slider global) et `threshCustom` (bool). Même idée que `L.auto` pour les noms : tant qu'un calque n'a pas eu son propre curseur touché, il suit le slider global en direct (`syncLayerThreshFromGlobal()`, appelé sur l'`input` de `#thresh`, met à jour `L.thresh` + le DOM de sa ligne sans tout re-render) ; dès qu'on le bouge, `threshCustom=true` et il se fige (icône `↺` pour revenir au global). Motivation : isoler une seule couleur (`sepMode='pick'` ou mute/solo) puis lui donner un seuil différent des autres calques, sans que ça n'affecte le reste. `process()` calcule `layerCutoffs = layers.map(L => (1-L.thresh/100)*maxEdge)` et l'utilise à la place de l'ancien `cutoff` unique dans la boucle de seuillage finale — chaque calque garde le même calcul de gradient/edge (inchangé), seul le seuil de sortie diffère. `rebuildLayers()` conserve `thresh`/`threshCustom` par index comme le reste (mute/solo/program), y compris entre deux images différentes.
- **Plage d'octaves en piano vu de dessus** (`#octaveStrip`, panneau Musique) : remplace les deux sliders "octave basse"/"octave haute" par une bande SVG (8 octaves de touches blanches/noires générées par `buildOctaveStrip()`) qu'on drag comme une sélection au marqueur (`pointerdown/move/up`, même idiome que le recadrage — `octaveFromEvent` = équivalent 1D de `normPointFromEvent`). `#octLow`/`#octHigh` restent la source de vérité, juste passés en `type="hidden"` : le strip ne fait que leur assigner une valeur et `dispatchEvent('input')`, donc `scaleNotesForRange()`, `resetControls()`, `CONTROL_DEFAULTS` etc. n'ont rien à savoir de son existence. `redrawOctaveHighlight()` repositionne un unique rect `#octHi` semi-transparent (pas de reclassement par touche) sur `input` de `octLow`/`octHigh` dans le hook `liveIds`. Pas de clic-pour-éditer ni de clavier sur ce contrôle (comme le recadrage) — c'est une sélection à la souris/tactile assumée.
- **Opacité image dans l'aperçu** (`#imgOpacity`, sous `.metaTransportRow`) : contrôle purement visuel, n'affecte jamais la détection de notes — `drawPreview()` applique `pctx.globalAlpha = imgOpacity/100` uniquement autour du `drawImage(adjCanvas,...)`, avant le fond noir semi-transparent fixe (`.55`, inchangé) et les points de notes (leur propre alpha via `layerAudible`, inchangé). À 100 (défaut, rétrocompatible) l'apparence est identique à avant ; à 0 l'image disparaît complètement et il ne reste que les points colorés sur fond noir. Câblé en dehors de `liveIds` volontairement : un `input` dédié qui ne fait que `syncLabels()+redrawPreview()`, pas `process()` — changer l'opacité d'affichage ne doit pas relancer toute l'analyse pixel par pixel. Inclus dans `PRESET_KEYS` malgré ça (les presets passent par `dispatchEvent('input')`, qui déclenche ce listener dédié normalement).
- **`.metaTransportRow`** (sous l'aperçu) : Direction/Courbe à gauche, Recadrage + résolution/notes à droite (`justify-content:space-between`, `.transportIcons` en premier dans le DOM) — l'ordre gauche/droite a été inversé une fois après coup sur demande explicite, donc si ça re-change, c'est bien `.metaTransportRow > .transportIcons` puis `> .metaCol` qu'il faut réordonner, pas le CSS `order`.
- **Zone Source fusionnée** (`#previewWrap`) : les 3 boutons Image/Vidéo/Webcam (`.srcBtn`) et le texte d'aide au glisser-déposer (`#dropHint`) vivent maintenant **à l'intérieur** de `#previewPlaceholder`, l'état vide du même cadre qui affichera ensuite le média chargé — regroupe ce qui était 3 blocs empilés (rangée de boutons, texte d'aide, cadre de preview) en une seule zone, pour gagner en hauteur et éviter le message redondant "dépose ici" / "ton image apparaîtra ici" (l'ancien texte `previewPlaceholder` a été retiré, le `dropHint` suffit maintenant à lui seul). Un seul `#fileInput` (`hidden`) partagé par Image et Vidéo — son `accept` est réglé juste avant `.click()` (`image/*` ou `video/*`), donc `loadFile()` et son autodétection MIME/extension restent inchangés ; ces boutons ne sont visibles qu'en état vide (dans `#previewPlaceholder`, masqué dès qu'un média est chargé), donc aucun risque de rouvrir le sélecteur de fichier par erreur en cliquant sur le média lui-même. Le glisser-déposer est monté sur **`#previewWrap`** (pas tout `.panel-source` comme avant) via `previewWrap.addEventListener('drop', ...)`, et reste actif que la zone soit vide ou déjà chargée — déposer un fichier remplace directement le média en cours (`loadFile()` appelle déjà `stopSource()` en premier, donc une webcam active est coupée avant le nouveau chargement, sans code dédié). `updateSrcBtns()` cible `wb.querySelector('span')` (pas `textContent` direct, pour ne pas effacer l'icône SVG du bouton Webcam) et bascule `.active` sur les 3 boutons selon `srcKind`. **Bouton « × » overlay** (`#removeMediaBtn`, `position:absolute` en haut à droite de `#mediaBox`, empilé après les canvases dans le DOM donc naturellement au-dessus sans z-index dédié) : seul point d'entrée visible pour vider le média une fois chargé (avec le drag&drop) — `removeMedia()` (facteur commun avec l'ancien code de `toggleWebcam()` qui gérait déjà "cliquer Webcam pour l'éteindre") route par `stopSource()`, qui coupe déjà les pistes de `camStream` et détache la `<video>` ; ce chemin devient le SEUL moyen de couper la webcam une fois active, puisque les 3 boutons (dont Webcam) sont masqués tant qu'un média est chargé. `aria-label` traduit (`removeMediaLabel`) posé dans `updateSrcBtns()` (appelée aussi au changement de langue) ; élément `<button>` natif donc focusable et activable au clavier (Entrée/Espace) sans code dédié. En vidant l'état, `clearPreview()` efface aussi le piano roll et le masque de sensibilité (`drawPianoRoll()`/`drawSensitivityMask()`, toutes deux déjà structurées pour clear-avant-check) — bug latent corrigé au passage : sans ça, un masque resté activé ou le dernier piano roll dessiné restaient visibles par-dessus le placeholder vide après un retrait, un cas bien plus atteignable maintenant que le × rend "vider le média" praticable en un clic sur n'importe quelle source (avant, seul le re-clic sur Webcam déclenchait ce chemin).
- **Hauteur max du média affiché** (`#mediaBox`, 490px) : les images très hautes (portrait 9:16) rendaient le cadre de preview démesurément long — demande explicite de l'utilisateur. `canvas#preview` est passé de `width:100%` (remplissage forcé, quelle que soit la hauteur résultante) à `max-width:100%; max-height:490px; width:auto; height:auto;` : un élément remplacé (canvas, comme `<img>`) avec ces seules contraintes se redimensionne tout seul en préservant son ratio intrinsèque, sans distorsion — donc toujours plein cadre en largeur pour une image large/carrée (comportement inchangé), mais plafonné en hauteur pour un portrait, largeur réduite en conséquence. Piège évité : `#sensMask`/`#scanOverlay`/`#removeMediaBtn` sont `position:absolute; inset:0` — s'ils étaient restés calés sur `#previewWrap` (qui reste toujours pleine largeur du panneau, un `<div>` bloc ne rétrécit pas pour suivre un enfant remplacé plus étroit), ils se seraient étirés au-delà du canvas désormais plus étroit et centré, désalignant masque/scan-line/bouton × par rapport à l'image réellement visible. Fix : un nouveau `#mediaBox` (`display:inline-block`, `max-height:490px`) enveloppe les 4 (canvas, sensMask, scanOverlay, removeMediaBtn) et se réduit en `shrink-to-fit` à la taille réelle du canvas ; `#previewWrap` centre ce bloc via `text-align:center` (pas de flex, pour ne pas perturber le layout bloc existant de `#previewPlaceholder`, resté un enfant normal de `#previewWrap`). L'eyedropper (`preview.addEventListener('click', ...)`) et l'éditeur de recadrage (`normPointFromEvent`) lisent déjà `preview.getBoundingClientRect()` directement (pas celui de previewWrap), donc aucun changement requis là — ils continuent de fonctionner correctement quelle que soit la largeur réelle du canvas.
- **Couleurs d'identité en Cyan/Magenta/Jaune** (`rebuildLayers()`, modes `hue`/`sobel`/`pick`) : la teinte synthétique par calque (`hslToHex(360*i/n, 70, 55)`, purement décorative — sert à distinguer les calques dans l'aperçu/piano roll, pas la vraie couleur détectée, voir `.swatchDetected` plus bas) est maintenant décalée de +180° (`wheelHue = (360*i/n + 180) % 360`) — à 3 calques, l'ancienne répartition égale sur la roue tombait pile sur rouge/vert/bleu (0°/120°/240°), la demande explicite était cyan/magenta/jaune à la place (180°/300°/60°, soit exactement l'ancien résultat plus un demi-tour de roue). Répartition toujours égale pour tout `n`, seul le point de départ change — un total de 6 calques donne toujours 6 teintes égales, juste toutes décalées d'un demi-tour par rapport à avant.
- **Expérimental : Calques/Instruments à côté du média** (`.sourceMediaRow`, dans `.panel-source`) : la section « Calques / Instruments » (titre + `#layerList`) a quitté le bas du panneau Analyse (colonne de droite, 1/5 très étroite — les noms d'instrument y étaient tronqués à 2 caractères, ex. "Ac") pour s'installer **à droite du média**, toujours à l'intérieur de la colonne Source (3/5 du `.rack`), pas comme une 4ᵉ colonne du rack. `.sourceMediaRow` utilise `grid-template-areas:"srcHead layersHead" "media layers"` (colonnes `2fr 1fr`, ratio demandé explicitement après un premier essai à largeur fixe `260px`) plutôt qu'un simple 2-colonnes, pour que les titres `<h2>Source</h2>`/`<h2>Calques / Instruments</h2>` (`.srcHead`/`.layersHead`) partagent la même rangée d'en-tête au lieu que le second traîne plus bas, dans `.layersPanel` — aussi demandé explicitement après le premier essai. Les deux `<h2>` gardent le style `.panel h2` générique (sélecteur descendant, s'applique même si `.layersPanel`/`.mediaCol` ne sont pas eux-mêmes des `.panel`) mais `margin:0` leur est réappliqué (spécificité 2-classes pour battre `.panel h2`) : l'espacement vient uniquement du `row-gap:14px` de la grille, pas de la marge native du `<h2>`. `.mediaCol` regroupe tout ce qui était avant en haut du panneau (`#fileInput`/`#freezeBtn`/`#srcHint`/`#previewWrap`) sous la zone `media` ; un item de grille ne fusionne pas ses marges avec ses enfants, donc `#previewWrap` a perdu son `margin-top` (le `row-gap` du parent suffit) et `#freezeBtn`/`#srcHint` ont chacun un `margin-bottom:10px` pour garder un espacement correct avec `#previewWrap` les rares fois où ils sont visibles (vidéo/webcam active, erreur de chargement). `align-items:start` pour que chaque colonne garde sa propre hauteur (le média plafonne à 490px, la liste de calques peut être plus ou moins haute selon leur nombre) sans s'étirer pour matcher l'autre. Sous 1080px, `.rack` repasse déjà en une colonne (règle existante) ; `.sourceMediaRow` suit la même logique avec ses propres `grid-template-columns:1fr`/`grid-template-areas` en pile (Source → média → Calques/Instruments → liste). Aucun changement JS : `$('layerList')`/`$('layerSwatchStrip')` etc. ne dépendent pas de la position DOM, seulement de l'id.
- **Instruments en icônes** (`.instrPicker` dans chaque ligne `.layerMain`, remplace le `<select>` visible) : la colonne resserrée par le layout ci-dessus tronquait déjà les noms d'instrument ("Ac", "El"…) — plutôt que rendre un `<select>` custom (perte du vrai menu déroulant natif, de la navigation clavier, du support lecteur d'écran), le `<select>` **reste natif et fonctionnel**, juste rendu invisible (`opacity:0`) et étiré en `position:absolute; inset:0` par-dessus une `<span class="instrIcon">` purement décorative (`pointer-events:none`) — tout clic/tap/focus passe donc par le vrai select, qui ouvre le vrai menu du système avec les vrais noms complets ; seul l'état "fermé" au repos affiche une icône au lieu du texte, ce qu'aucune combinaison CSS ne permet de faire directement sur un `<select>` natif. `renderLayerList()` construit `.instrPicker` (icône + select) et une fonction locale `syncInstrIcon()` retape `instrIcon.textContent` + `instrPicker.title` (tooltip = nom traduit, seul endroit où le nom complet reste visible au repos) à chaque `change` du select ainsi qu'à la construction initiale — donc toujours synchronisé après un changement d'instrument, un preset, ou un changement de langue (`rebuildLayers()` re-render tout). `.layerMain` passe de deux colonnes `1fr` (nom + select) à `1fr 32px` (nom + icône carrée) : le nom de calque récupère tout l'espace que l'ancien `<select>` texte lui prenait.
  - **Icônes = emoji natifs, pas du SVG dessiné à la main** : premier essai en SVG maison (dessiné à la main, zero-dépendance) jugé pas assez ressemblant aux vrais instruments par l'utilisateur ("t'as pas des icônes plus proches des instruments en stock ?"). Remplacé par un champ `emoji` sur chaque entrée de `GM_INSTR` (ex. `🎹`/`🪈`/`🥁`), rendu tel quel via `instrIcon.textContent` — choix explicite après avoir mis en balance 3 options (emoji natifs / vraie lib d'icônes via CDN / retouche du SVG maison) : emoji gardent le zero-dépendance (rendu par la police système, aucun fichier à charger) et sont objectivement plus reconnaissables qu'un glyphe 18px dessiné à la main, au prix d'un style plus coloré/illustratif qui tranche avec le reste du thème (trait fin monochrome). `.instrIcon` n'a plus de `color` (les emoji couleur ignorent la propriété CSS `color`), juste `font-size:17px` pour le dimensionnement.
  - **Liste réduite à 6 instruments (une seule collision d'emoji)** : la liste GM initiale comptait 19 entrées ; à l'usage l'utilisateur a remarqué qu'"beaucoup se ressemblent trop" au **son**. Cause racine trouvée dans `timbreFor()` (le synthé Web Audio d'aperçu, plus bas dans le fichier) : plusieurs entrées de la liste tombaient dans la **même branche de timbre** et sonnaient identiques au bit près (Célesta/Vibraphone/Marimba/Boîte à musique/Cloches tubulaires/Xylophone partageaient tous la plage `prog 8-15` "mallets" ; Piano acoustique/électrique/Clavecin tombaient tous dans le `default` faute de branche dédiée). Plutôt qu'élargir `timbreFor()` avec plus de branches, la liste `GM_INSTR` a été réduite à exactement **6** (le nombre max de calques, `#layers` plafonne déjà à 6) en piochant **un seul représentant par branche distincte** : Piano acoustique (`default`), Orgue (`16-23`, sawtooth quasi 100% sustain), Vibraphone (`8-15`, triangle percutée sans sustain), Basse synthé (`32-39`, sine+sub, coupure grave), Flûte (`72-79`, sine soutenue et brillante), Steel drums (`===114`, seule branche en onde carrée). Bénéfice de bord : `rebuildLayers()`'s `GM_INSTR[i % GM_INSTR.length].prog` (déjà générique, aucun changement de code) donne maintenant un instrument par défaut **différent pour chacun des 6 calques max**, plus de doublon par défaut. Bénéfice de bord n°2 : sur seulement 6 entrées, une seule paire d'emoji se partage encore un symbole (Piano/Orgue → `🎹`, tous deux réellement des instruments à clavier) — Basse synthé récupère `🎚️` (fader/mixage) plutôt que de retomber sur `🎹` comme un 3ᵉ clavier.
- **Presets** (`#presetBar`, entre le header et `.rack`) : `PRESET_KEYS` liste les ~20 contrôles couverts (tout Analyse + Musique + `playDir`/`curve`, **pas** mute/solo qui restent un état de mix éphémère comme documenté plus haut). `currentPresetValues()` lit ces contrôles **plus** un `layerOverrides` par calque (`program` + `thresh`/`threshCustom`, la sensibilité par calque) — c'est directement la demande initiale de l'utilisateur ("je vois vite les limites quand je veux travailler que sur une couleur") qui a motivé le fait d'inclure le per-layer dans les presets, pas juste les réglages globaux. Deux sources dans le même `<select>` : `BUILTIN_PRESETS` (tableau codé en dur dans `index.html`, noms bilingues `{fr,en}` — c'est le fichier que l'utilisateur édite lui-même pour ajouter ses presets « maison », `value="builtin:<index>"`) et les presets utilisateur (`localStorage['dessons.presets']`, JSON `{nom: {values, layerOverrides}}`, `value="user:<nom>"`). `applyPreset()` réutilise le pipeline existant partout où c'est possible (assigne puis `dispatchEvent('input')` sur chaque contrôle, comme `resetControls()`) — **sauf** `playDir`/`curve` : ce sont des `<input type="hidden">` pilotés par les boutons-icônes, pas par `liveIds`, donc `dispatchEvent('input')` dessus n'a aucun écouteur à déclencher ; `applyPreset()` les assigne directement puis appelle `renderTransportIcons()`/`toggleCurveEditor()` à la main. Enregistrer demande juste un nom (`prompt()`, pas de modale dédiée — cohérent avec le reste de l'app qui n'a pas de système de dialogue). `renderPresetOptions()` est appelée dans `setLang()` pour retraduire les noms des presets fournis et les libellés de groupe. **Export/Import** (`#presetExportBtn`/`#presetImportBtn`) : `localStorage` ne quitte jamais un couple navigateur+origine donné (pas de sync, rien entre `file://` et GitHub Pages, rien entre deux navigateurs) — Export sérialise `getUserPresets()` tel quel en `.json` téléchargeable (même mécanisme blob+`<a download>` que `buildMidi()`), Import relit ce JSON et fait un `Object.assign` dans les presets existants (donc un import = un merge, pas un remplacement total ; collision de nom → `confirm()` listant le nombre concerné avant d'écraser). Aucune validation de forme au-delà de "c'est un objet plain" — un JSON corrompu déclenche juste l'alerte d'erreur au `JSON.parse`, un JSON valide mais aux clés farfelues sera silencieusement ignoré champ par champ par `applyPreset()` (déjà tolérant aux clés manquantes).
- **Grille temps/hauteur plutôt qu'un point par pixel** : l'image est découpée en `steps` divisions (temps) × N voies (hauteur, une par degré de la gamme choisie dans la plage d'octaves choisie). Ça évite d'avoir des centaines de micro-notes injouables, et ça garantit qu'on reste toujours sur la gamme (pas de fausses notes possibles).
- **Sens de lecture réglable, cardinal + diagonal** (`playDir` : `lr`/`rl`/`tb`/`bt`/`tlbr`/`brtl`/`trbl`/`bltr`) : le temps peut suivre n'importe lequel des 8 axes/sens d'une image (les 4 côtés + les 2 diagonales, dans les 2 sens). Modèle unifié dans `DIR_DEFS` (`readDir()` renvoie l'entrée courante) : chaque direction est une paire de fonctions affines `fwd(nx,ny) -> [τ,ρ]` / `inv(τ,ρ) -> [nx,ny]` sur des coordonnées image normalisées (`nx,ny ∈ [0,1)`, `τ`=fraction de temps, `ρ`=fraction de hauteur), `inv` étant l'inverse algébrique exact de `fwd` (vérifié par test). Convention hauteur : pour les 4 cardinales, top/droite = aigu (comme avant) ; pour les 4 diagonales, le calque de hauteur est l'AUTRE diagonale, avec le côté haut-droite = aigu — même logique « haut/droite = aigu » appliquée à 45°. `process()` produit toujours des notes `{step, midi, vel}` où `step` = ordre de lecture (0 = premier joué) : l'export `.mid` **et** le scheduler temps réel consomment `step` tel quel, direction-agnostiques — seuls `process()` (`fwd`, un seul passage pixel par pixel, plus de découpage rectangulaire par cellule) et `drawPreview()`/`drawPlayhead()` (`inv`, placement visuel) en tiennent compte. La tête de lecture et les points d'aperçu sont un simple segment `inv(τ,0)→inv(τ,1)` (droit même en diagonale, car `inv` est affine) ; le canvas clippe tout seul ce qui déborde du cadre près des coins.
- **Courbe de lecture** (`curve` : `linear`/`accel`/`decel`/`pulse`/`swing`) : `curveFrac(mode, k, n)` renvoie la fraction de boucle (0..1) où le pas `k` sonne (`stepTimeFrac(k,n)` = wrapper qui lit `$('curve').value`). Linéaire = `k/n` ; les autres courbent ça (accélérando, ritardando, dense-aux-bords, swing 1 pas sur 2). Consommé par le scheduler (durée de chaque pas = `loopMs * (stepTimeFrac(k+1) - stepTimeFrac(k))`), `drawPlayhead()` (via le ring buffer `stepWin` de `{step, at, span}`), `drawPreview()` (ticks sur le bord d'attaque) **et `buildMidi()`** (positions de tick courbées — la groove est baclée dans le `.mid`, contrairement au mute/solo). `linear` reproduit exactement l'ancien comportement.
- **`playDir` et `curve` = boutons-icônes** (pas des `<select>`) rendus par `renderTransportIcons()` à côté du bouton « Écouter l'aperçu » (panneau gauche). L'état vit dans deux `<input type="hidden">` (`#playDir`, `#curve`) → tous les `$('playDir').value` / `$('curve').value` marchent tels quels. `DIRS` liste les 8 directions (4 cardinales + 4 diagonales) avec leur tracé SVG (flèche/chevron, même style que les cardinales tourné à 45°) ; clic → `process()`. Clic courbe → `redrawPreview()` (6 ticks placés par `curveFrac`, l'icône EST la groove). `.iconGroup{flex-wrap:wrap}` en filet de sécurité pour les 8 boutons sur petit écran. Re-render sur changement de langue (tooltips).
- **Courbe `custom`** : `customPoints = [0,y1,y2,y3,1]`, 3 poignées libres à x fixe (.25/.5/.75). `customCurveY(p)` = interpolation linéaire par morceaux. Monotonie **garantie par construction** : au drag, `customPoints[i]` est clampé entre `customPoints[i-1]` et `customPoints[i+1]` (jamais l'inverse — on ne touche jamais aux voisins). Éditeur SVG (`#curveGraph`, 3 `<circle class="cp">`) câblé une fois par `initCurveEditor()` (pointer events + `setPointerCapture`), affiché/masqué par `toggleCurveEditor()`. Premier passage sur « Personnalisée » → `customPoints` initialisé depuis la courbe précédemment active (`customSeeded` évite de re-seeder ensuite). Ne jamais permettre à l'utilisateur de rendre la courbe non-monotone (durées de pas négatives → scheduler cassé).
- **Piano roll** (`#pianoRoll`, sous Direction/Curve dans la colonne Source) : remplace le compteur « X notes » par une grille step × hauteur qui redessine **`lastNotes`** tel quel (rien recalculé — le tableau qui sert déjà à `buildMidi()` et au scheduler temps réel). Axe X **linéaire en index de step**, pas en temps courbé par `curve` : c'est ce qui le fait lire comme un piano roll normal plutôt que comme un graphe déformé ; seule la tête de lecture suit la position réelle (courbée) de la boucle, donc une courbe « Pulsing » se voit à la tête qui s'agglutine aux bords de la grille sans que les notes elles-mêmes ne bougent. Largeur de note = Gate% (comme le calcul réel de durée de note dans le scheduler/export), couleur = celle déjà assignée au calque d'origine (aucune palette parallèle). `currentLoopPos(steps)` a été extrait de `drawPlayhead()` pour être partagé entre la tête de lecture sur l'image et celle du piano roll — les deux lisent le même ring buffer `stepWin`, donc jamais en léger désaccord l'une avec l'autre.
- **Masque de sensibilité en direct** (`#sensMask`, bouton `#maskToggleBtn`) : overlay cyan semi-transparent sur l'aperçu montrant exactement quels pixels franchissent le seuil `Sensibilité` actuel. `drawSensitivityMask()` reseuille **`lastEdgeArr`/`lastMaxEdge`** (le tableau de magnitude de gradient déjà mis en cache par `process()` pour le bouton Otsu, voir plus haut) — aucun recalcul de gradient/Sobel/LUT ici, uniquement une comparaison + un `putImageData`, donc c'est assez bon marché pour tourner à chaque tick de drag du slider sans lag perçu. Masqué par défaut (`maskVisible=false`) ; le bouton bascule `hidden` sur `#sensMask` en même temps que l'état, pas de recalcul de la séparation en calques déclenché par ce toggle.
- **Courbe de tons interactive avec histogramme** (`#toneCurveGraph`, remplace visuellement les 3 sliders Black/White/Gamma — **remplacement explicite demandé**, contrairement au principe général « en plus, jamais à la place » qui s'applique aux 5 autres items de ce brief). Le graphe ne fait que lire/écrire `#blackPt`/`#whitePt`/`#gamma` (toujours la source de vérité, affichés en chiffres à côté) et appelle **`buildToneLUT()`**, la fonction déjà utilisée par `process()` — donc rétrocompatibilité garantie par construction, pas par test a posteriori : la courbe affichée EST la LUT réellement appliquée. `computeHistogram()` (luminosité 0-255, pondération standard 0.299/0.587/0.114) tourne **une seule fois par chargement d'image** (pas par frame), avec une échelle en racine carrée sur l'axe Y pour qu'un pic unique (fond uni) n'écrase pas visuellement le reste de la distribution. 3 poignées glissables : noir (bas-gauche, sur l'axe X), blanc (haut-droite), gamma (point milieu de la courbe, dérivé algébriquement de `0.5^(1/gamma)` — pas de recherche numérique, la position de la poignée et la valeur de `gamma` s'inversent exactement l'une dans l'autre). Le drag ne fait que `dispatchEvent('input')` sur les inputs sous-jacents, donc `resetControls()`/presets/`liveIds` n'ont rien à savoir de l'existence du graphe.
- **Animation de balayage sur Direction/Courbe** (`#scanOverlay`, canvas superposé à l'aperçu) : au survol de `.transportIcons` (le groupe de boutons Direction+Curve), une ligne cyan balaie la vignette réellement chargée dans le sens choisi, cadencée par la courbe choisie — démo visuelle de ce que fera la vraie tête de lecture, sans avoir à lancer la lecture. Réutilise **`dir.inv()`** et **`curveFrac()`**, les mêmes fonctions que `drawPlayhead()`/`buildMidi()` (aucune maths dupliquée, donc la démo ne peut pas mentir sur le comportement réel). Boucle en `requestAnimationFrame` uniquement pendant le survol (`mouseenter`/`mouseleave` sur `.transportIcons`) — jamais un timer permanent en tâche de fond — et s'interrompt d'elle-même dès qu'une vraie lecture démarre (`scanFrame()` retourne tôt si `playing`), pour ne jamais se superposer à la vraie tête de lecture. `#scanOverlay` a `pointer-events:none` : ça le rend invisible à `elementFromPoint()`/aux tests d'interaction DOM (attendu, pas un bug), il faut vérifier son rendu par capture d'écran ou lecture directe du canvas, pas par un hit-test. `hoverDir`/`hoverCurve` (posés/effacés par les `mouseenter`/`mouseleave` de chaque bouton individuel dans `renderTransportIcons()`) : tant que la souris est sur UNE icône précise (pas encore cliquée), `scanFrame()` la préfère à `readDir()`/`$('curve').value` — sans ça, le balayage montrait toujours le sens/la courbe **déjà actifs** en survolant n'importe quelle icône du groupe au lieu de prévisualiser celle réellement survolée (bug rapporté par l'utilisateur). Remis à `null` dans `stopScanAnim()` (sortie du groupe entier) pour ne jamais garder une valeur survolée périmée d'une session de survol à l'autre.
- **Clavier sous Gamme/Note fondamentale** (`#scaleKeyboard`) : mini clavier une octave (12 touches), construit une fois (`buildScaleKeyboard()`) puis simplement re-teinté (`redrawScaleKeyboard()`) à chaque changement de `scale`/`root` — chaque `<rect>` porte un `data-semi` fixe, aucune reconstruction DOM au changement. Les degrés de la gamme (`SCALES[scale].map(d => (root+d)%12)`) reçoivent `--accent` à 50% d'opacité, la fondamentale reçoit `--cyan` à 100% — couleur ET intensité différentes, comme demandé, en réutilisant les deux teintes déjà dans la palette plutôt qu'une nouvelle couleur. Aucun calcul d'image impliqué (contrairement à tout le reste de cette section) : c'est une pure fonction gamme+fondamentale → indices de touches.
- **Pastilles de couleur détectée par calque** (`.swatchDetected` dans chaque ligne de « Calques / Instruments », `#layerSwatchStrip` sous le slider « Séparation en calques ») : distinctes de la pastille d'identité existante (`.swatch`, la couleur arc-en-ciel/synthétique par index utilisée pour les points de l'aperçu et du piano roll — qui reste inchangée, ces pastilles ne remplacent rien) — celle-ci montre la **vraie couleur moyenne** des pixels effectivement tombés dans le bucket de ce calque. Accumulée dans `process()`, dans la même passe pixel-par-pixel qui calcule déjà `layerOf(px)` (pas de second passage sur l'image) : `sumR/sumG/sumB/swCount` par calque, moyennés en `L.detectedColor` juste après la boucle. Moyenne sur **tous** les pixels assignés au calque (le bucket entier), pas seulement ceux qui passent le seuil de sensibilité et deviennent des notes — un calque à seuil très strict reste donc lisible (pas de pastille qui vire au noir) puisqu'elle répond à « qu'y a-t-il dans ce calque » plutôt que « qu'est-ce qui sonne là, maintenant ». `null` (bucket vide, aucun pixel n'y est jamais tombé) rendu en transparent — signal utile en soi (ex. teinte bleue jamais présente dans l'image). Mis à jour à chaque `process()` (donc aussi en direct sur vidéo/webcam) via `updateLayerSwatches()`, qui ne fait que retaper le `background` des éléments déjà en place (même idiome que `syncLayerThreshFromGlobal()`) — `renderLayerList()` ne reconstruit les pastilles (et le nombre de chips du strip) qu'au changement structurel du nombre de calques.
- **Lien croisé Image ↔ Piano roll ↔ Calques** (brief dédié, cinq points) :
  - **Isolation par calque** (`hoverLayer`/`lockedLayer`, `isolatedLayer()`, `layerVisualAlpha(i)`) : survoler une ligne `.layer` dans « Calques / Instruments » assombrit (alpha 0.2) les points/notes des AUTRES calques sur l'image ET le piano roll, indépendamment de mute/solo (`layerAudible()` reste la source de vérité pour le mix réel, `layerVisualAlpha()` ne fait qu'ajouter cette dimension purement visuelle par-dessus quand `isolatedLayer()>=0`). Cliquer une ligne **verrouille** l'isolation (persiste même si la souris s'en va) — reclique la même ligne, ou une autre, pour changer/annuler ; `lockedLayer` prime sur `hoverLayer`. Écouteurs posés sur `row` dans `renderLayerList()` ; le `click` ignore `e.target.closest('button, input, select')` pour ne pas entrer en conflit avec M/S, le nom, l'instrument ou le seuil — la seule vraie zone de clic "fond de ligne" est le label du nom (voir plus bas) et le label "Threshold". `onLayerIsolationChange()` redessine les deux canvases et retape la classe `.isolated` sans reconstruire la liste. `renderLayerList()` referme `lockedLayer` à -1 s'il dépasse le nouveau nombre de calques (changement de layer count) et remet toujours `hoverLayer` à -1 (la souris n'est pas sur une ligne fraîchement recréée).
  - **Surbrillance par note** (`hoverNote`, `setHoverNote()`, `sameNote()`) : indépendant de l'isolation par calque ci-dessus — survoler UNE note précise (sur l'image, `mousemove` sur `#preview` avec recherche du point le plus proche dans un rayon de 15px² ; sur le piano roll, `mousemove` sur `#pianoRoll` avec un calcul direct de cellule `step`/`pIdx`, même arithmétique que `drawPianoRoll()`) dessine un anneau blanc à opacité 1 (jamais atténué, même si son calque est isolé/dimmed par ailleurs) sur le point/la cellule correspondante **dans l'autre visuel** — implémenté dans les deux sens comme demandé ("si techniquement possible"). `setHoverNote()` compare avec `sameNote()` avant de redessiner, pour ne pas relancer `redrawPreview()`/`drawPianoRoll()` à chaque pixel de `mousemove` tant que la souris reste dans la même cellule/le même point. Le hit-test image ignore le survol si `cropEditing`/`pickLayer>=0` (ne doit pas interférer avec le recadrage ou la pipette).
  - **Tooltips M/S + `aria-label`** : `title`/`aria-label` identiques et déjà descriptifs existaient pour Mute/Solo (`muteTitle`/`soloTitle`) — seul `aria-label` manquait, ajouté en miroir du `title`. Nouveau : la pastille d'identité (`.swatch`) n'avait de tooltip qu'en mode `pick` (`swatchTitle`, l'action "cliquer pour choisir") ; hors pick elle n'en avait aucun — ajout de `swatchIdentityTitle` ("Couleur d'affichage") pour l'état désactivé. `.swatchDetected` avait déjà un tooltip, complété d'un `aria-label` miroir. Les deux pastilles étaient déjà de formes différentes avant ce brief (`.swatch` carré arrondi 3px, `.swatchDetected` cercle 50%) — rien à changer là, la demande était déjà satisfaite par un travail précédent.
  - **Nom de calque en label + édition au double-clic** (`.layerNameLabel`/`.layerNameEdit`, `beginEditName()`) : remplace l'ancien `<input type=text>` toujours visible par un `<span>` texte simple au repos (même look qu'un label, pas une boîte de formulaire) qui swap vers un vrai `<input>` au double-clic — pattern identique à `beginEditVal()` pour les valeurs numériques (Enter/blur valident, Escape annule via `box.dataset.cancel`). Le `<span>` n'étant PAS dans `closest('button, input, select')`, un simple clic dessus déclenche aussi le toggle d'isolation ci-dessus (voulu — double-clic = 2 clics + 1 dblclick, l'isolation clignote on/off puis l'édition démarre, effet secondaire mineur accepté). **Suggestion de nom par rôle** : `rebuildLayers()` calcule maintenant `program` avant le nom (au lieu qu'après), et si `PERCUSSIVE_PROGRAMS` (`Set([11,114])`, Vibraphone/Steel Drums parmi les 6 instruments actuels) contient ce programme, l'auto-nom devient `t('layerRoleRhythm')` ("Rythme") au lieu de "Calque N" — uniquement pour un calque encore en mode auto (`isAuto`), jamais si l'utilisateur a déjà renommé. **Persistance Preset** : `currentPresetValues()`/`applyPreset()` incluent maintenant `name`/`auto` dans `layerOverrides` (à côté de `program`/`thresh`/`threshCustom` déjà là) — un preset ancien sans ces champs est toléré sans erreur (`if(ov.name != null)`), un calque frais garde alors son nom auto-suggéré.
  - **Compteur de notes par calque** (`.layerNoteCount` dans `.layerThresh`, `updateLayerNoteCounts()`) : `lastNotes[i].length` — déjà exactement le tableau que `buildMidi()`/le scheduler consomment — juste affiché, aucun recalcul séparé. Même idiome que `updateLayerSwatches()` (DOM-only, appelé juste après elle à la fin de `process()`) : ne reconstruit rien, donc reste bon marché même en direct sur vidéo/webcam à ~15 fps.
  - **Sous-sections pliables dans Analyse de l'image** (`.accordionSection`/`.accordionBody`/`.sectionAccordionBtn`, `initSectionAccordion()`) : réutilise tel quel le bouton-accordéon déjà en place pour « Comment connecter à Logic » (même classe `.accordionBtn`, même flèche rotative sur `aria-expanded`) plutôt que d'inventer un nouveau style d'en-tête de section. Deux blocs — « Couleur & tons » (Méthode, Courbe de tons, Saturation, Inverser) et « Détection » (Sensibilité+Auto/Masque, Lissage, Séparation en calques, Tolérance couleur, Opacité image) — tous deux **ouverts par défaut** à chaque chargement (pour ne jamais cacher un réglage à quelqu'un qui n'a encore rien touché), mais le choix plié/déplié une fois fait par l'utilisateur est mémorisé dans `localStorage['dessons.sections']` (clé = l'id du bouton) et relu au prochain chargement — donc un rechargement respecte un pli volontaire, mais un état "jamais touché" reste toujours ouvert. Purement une réorganisation visuelle : aucun id de contrôle déplacé, `resetAnalysis`/`liveIds`/`PRESET_KEYS` etc. n'ont rien à savoir de cette restructuration DOM.

## Structure du fichier `index.html`

- CSS en haut : thème "rack audio" sombre, calqué sur l'identité visuelle de **neoyume.com** (le site perso de l'utilisateur, dossier voisin `2026_00_neoyume_website/neoyume.com`) plutôt qu'un dark mode générique. Palette identique (mêmes hex) : `--bg`/`--glow` (fond + halo radial `radial-gradient(120% 100% at 15% -10%, var(--glow), var(--bg) 55%)`), `--panel`/`--panel-2`, `--line`, `--text`/`--text-dim`, `--accent` (ex-`--amber` — variable renommée partout via un remplacement global, pas juste la valeur, pour ne pas laisser un nom trompeur ; d'abord un corail `#ff5f7e` repris de neoyume.com, puis passé en cyan pur `#00ffff` sur demande explicite de l'utilisateur — `--accent-dim` a suivi en cyan sombre `#0a6670`), `--cyan` (`#4dd9c9`, le cyan plus doux de neoyume.com, resté inchangé — les deux accents sont désormais tous deux dans la famille cyan, distingués par la saturation : `--accent` vif pour le CTA principal, `--cyan` plus doux pour le secondaire). Police `--sans` = **DotGothic16** (Google Fonts, `<link>` dans `<head>` — le site source la sert via `@fontsource` avec build Astro, indisponible ici en zero-build, donc CDN) appliquée à toute l'UI ; `--mono` reste un vrai monospace pour les valeurs numériques des sliders (contraste, notes, ports MIDI) où l'alignement des chiffres prime sur la fidélité stylistique. Overlay de grain (`#ny-noise`, canvas plein écran `position:fixed`, redessiné en bruit aléatoire par pixel à chaque frame, `mix-blend-mode:overlay`, `pointer-events:none`) : version simplifiée du `NoiseOverlay.astro` du site — la texture de grain est portée, mais pas le spot lumineux qui suit la souris, le glitch de chromatic aberration au scroll rapide, ni le curseur personnalisé (gadgets d'un site portfolio, pas demandés, et potentiellement gênants pour la précision requise en draguant un slider ou le rectangle de recadrage). Piège classique du reset CSS à connaître si on retouche ce thème : les éléments de formulaire (`button`/`input`/`select`/`textarea`) **n'héritent pas** de `font-family` par défaut dans les feuilles UA des navigateurs — sans la règle `button, input, select, textarea{font-family:inherit;}` (juste après `*{box-sizing:border-box;}`), la moitié des boutons/sliders retombent silencieusement en Arial malgré `body{font-family:var(--sans)}`. Repéré après coup via un audit `getComputedStyle` sur tous les contrôles, pas visible à l'œil sur les gros boutons.
- **Header réduit** : `<h1>Dessons</h1>` seul (taille doublée, 44px), plus de tagline sous le titre — la bannière `.flowSteps` juste en dessous fait déjà ce travail (plus courte, plus concrète). Clé i18n `tagline` supprimée des deux langues.
- **Disposition desktop en 3 colonnes** (`.rack{grid-template-columns:1fr 3fr 1fr}`, 1/5-3/5-1/5) : Musique à gauche, Source + aperçu au centre (large), Analyse de l'image à droite — remplace l'ancien 2-colonnes (Source | tout le reste). Le centre (Source) porte : la source, le sens/la courbe de lecture **juste sous l'aperçu image**, le recadrage, puis une `.actionRow` (grille 3 colonnes) avec `Exporter le .mid` / `Écouter l'aperçu` / `Jouer dans Logic` côte à côte — les 3 façons de "faire quelque chose" avec l'image, chacune avec son statut (dot+texte ou hint) juste dessous. `Calques/Instruments` a rejoint le panneau **Analyse** (à droite, en bas), pas Source — les réglages de calque (M/S, seuil par calque, instrument) sont conceptuellement plus proches de "comment on analyse" que de "où est l'image". Les `.row2` (paires de sliders côte à côte) ont été déballées en piles simples dans Musique/Analyse — devenues trop étroites pour label+slider+valeur une fois à 1/5 de largeur. En dessous de 1080px, `.rack` repasse à 1 colonne mais l'ordre de lecture reste `panel-source` → `panel-analysis` → `panel-music` via `order` CSS (le DOM, lui, est Musique/Source/Analyse — l'ordre visuel voulu sur desktop) : on voit l'image avant de régler quoi que ce soit, même empilé.
- **En-tête** : `<h1>Dessons</h1>` (cyan pur, `color:var(--accent)`) → `.flowSteps` (le bandeau 1-2-3, maintenant DANS `<header>`, plus en dessous) → `#langToggle`, dans cet ordre, en une seule ligne flex (`header{display:flex}`, plus de `position:absolute` sur `.langToggle`). `.flowSteps{flex:1 1 260px}` prend l'espace restant entre le titre et FR/EN.
- **Étape 3 du bandeau = les vrais boutons Export/Play** (expérimental, sur demande explicite de l'utilisateur — "on va tenter un truc") : `#exportBtn` et `#playBtn` (avec leur icône, et pour chacun leur hint/statut — `#exportDisabledHint`, le `.midiRow` MIDI) vivent maintenant **physiquement** dans `.flowSteps .step3`, pas dans le panneau Source — ce sont les mêmes éléments avec les mêmes ids que partout ailleurs dans le code (`process()`, `startPlayback()`, `updateTransportButtons()` etc. n'ont pas changé, `$('exportBtn')`/`$('playBtn')` continuent de les trouver où qu'ils soient dans le DOM). Étapes 1/2 sont redevenues du texte simple (`flowStep1`/`flowStep2`, remplace l'ancienne clé unique `flowSteps` en HTML) puisque l'étape 3 n'est plus un texte à traduire mais des vrais contrôles. `#previewBtn` (Écouter l'aperçu), lui, a rejoint `.metaTransportRow` comme 3ᵉ colonne (après Direction/Courbe et Recadrage/résolution) — `.actionCol{flex:none}` évite qu'il s'étire, et comme `.metaCol` a `flex:1`, il absorbe tout l'espace restant et pousse naturellement ce 3ᵉ bloc contre le bord droit sans qu'on ait besoin de `justify-content` dédié. L'ancien `.actionRow` (grille 3 colonnes Export/Preview/Play) a disparu, ses 3 boutons vivent maintenant à deux endroits différents.
- JS, dans l'ordre :
  1. **i18n** : dictionnaire `I18N = {fr, en}`, helper `t('clé')` pour les chaînes dynamiques, `[data-i18n]` / `[data-i18n-html]` pour le statique. `setLang()` réapplique tout (titre, `<html lang>`, calques auto, libellés instruments, état MIDI) et mémorise. `initialLang()` : `localStorage` puis `navigator.language`. Bouton `#langToggle`.
  2. Constantes : `GM_INSTR` (instruments General MIDI, `{fr, en, prog}`), `SCALES` (intervalles par gamme)
  3. **Source** (`srcEl`, `srcKind` ∈ `image|video|webcam`) : `loadFile()` gère image *et* vidéo, `toggleWebcam()` (`getUserMedia`), `fitCanvas()` dimensionne les canvas, `grabFrame()` copie la frame courante dans `imgCanvas`, `liveLoop()` re-analyse vidéo/webcam en `setTimeout` ~15 fps (sauf si `frozen`). `stopSource()` = filet de nettoyage (stop tracks caméra, pause vidéo, clear timer).
  4. Gestion des calques (`rebuildLayers`, `renderLayerList`) — couleur HSL ; nom auto régénéré selon la langue tant que l'utilisateur n'a pas renommé (`L.auto`)
  5. `readDir()` + `process()` : le cœur — lit `imgCanvas` (peu importe la source), pré-traite (inversion → flou → courbe de tons/LUT → saturation, cf. plus haut), calcule luminosité/gradient(ou Sobel)/teinte par pixel, assigne à un calque (avec postérisation optionnelle de la teinte), puis un seul passage pixel par pixel classe chaque pixel dans sa cellule temps×hauteur via `DIR_DEFS[playDir].fwd` (marche pareil pour les 8 directions, cardinales ou diagonales) et applique le seuil. Sortie : `lastNotes[calque] = [{step, midi, vel}]`, `step` = ordre de lecture.
  6. `drawPreview()` : dessine la frame courante + points de notes colorés par calque (placement selon `readDir()`)
  7. `buildMidi()` / `writeVarLen()` / helpers binaires : génère le fichier `.mid` en `Uint8Array`
  8. Transport & sorties : un seul moteur (scheduler + tête de lecture), deux **sinks** via la variable `sink` :
     - `sink === 'midi'` — Web MIDI temps réel : `ensureMidi()` (lazy `requestMIDIAccess`, au 1er clic), `refreshPorts()` (auto-sélectionne un port `/IAC|bus/i`), `setMidiState(clé, on, arg)` / `renderMidiState()` (statut re-traduisible, `{x}` = port), `midiOut.send(bytes, timestamp)` en horloge `performance.now()`. `stopPlayback` : `midiOut.clear()` + note-off explicites (`hangingNotes`) + CC 120/123 (`usedChannels`).
     - `sink === 'synth'` — **synthé d'aperçu Web Audio** (zéro dépendance) : bouton « Écouter l'aperçu » (panneau de gauche). `ensureAudio()` (crée l'`AudioContext` au 1er clic + compresseur + petite réverb `makeImpulse`), `timbreFor(prog)` (famille GM → forme d'onde + ADSR + filtre), `spawnVoice()` (osc + biquad + enveloppe, `+sub` pour les basses, plafond 96 voix), `stopAllVoices()`. L'horloge audio est verrouillée sur `performance.now` à chaque tick du scheduler (`audioEpoch`).
       **iOS Safari** (deux bugs distincts, corrigés séparément — l'utilisateur a confirmé que le premier seul ne suffisait pas) :
       1. *Déblocage de geste* : le handler de clic de `#previewBtn` est **volontairement synchrone** (pas `async`/`await`) — `ensureAudio()` crée le contexte *et* démarre un buffer muet d'1 sample de façon synchrone dans le même geste utilisateur, puis `startPlayback('synth')` enchaîne aussitôt sans attendre `ac.resume()` (appelé en fire-and-forget, `.then(updateSynthState)`). Ne jamais réintroduire un `await` entre le clic et le premier `osc.start()`.
       2. *Switch sonnerie/silence* : Safari traite la sortie Web Audio normale (`ac.destination`) comme un son « ambiant » et la coupe quand l'interrupteur physique est sur silence — **contrairement** à un élément `<audio>`/`<video>`, qui utilise la catégorie « playback » non coupée par ce switch. Contournement dans `ensureAudio()` : sur iOS (`isIOS()` — UA `iPad|iPhone|iPod`, ou heuristique iPadOS 13+ qui se fait passer pour un Mac via `maxTouchPoints>1`), le graphe audio est routé vers `ac.createMediaStreamDestination()` puis lu par une balise `<audio>` cachée (`iosAudioEl`, gardée en référence pour ne pas être GC'ée) au lieu de `ac.destination` directement. Ailleurs (desktop), chemin direct inchangé — pas de latence ajoutée.
       **Diagnostic visible** : ligne d'état sous `#previewBtn` (`#synthState`/`#synthDot`, mêmes classes CSS que `.midiRow`/`.dot`) affichant `ac.state` (`running`/`suspended`/`closed`) — `updateSynthState()`, appelée depuis `ensureAudio()`, après resolution de `ac.resume()`, et dans `updateTransportButtons()`/`updatePlayBtnState()`. Si un jour ça re-coince sur iOS, regarder d'abord cette ligne avant de deviner.
     Commun : `stepDurationMs()`, `scheduler()` (**planification juste-à-temps** : `setInterval` 25 ms, lookahead 150 ms, relit `lastNotes`/tempo/gate à chaque pas → édition live ; resync si gros retard ; branche sur `sink`), `startPlayback(which)` / `stopPlayback()`, `updateTransportButtons()`, `drawPlayhead()` (rAF, position `playStep`/`nextStepTime`, axe+sens selon `readDir()`). Les deux boutons sont exclusifs (lancer l'un stoppe l'autre). `visibilitychange` coupe le transport et met `liveLoop` en pause.
  9. Listeners : tout est recalculé en live (`process()`) à chaque changement de slider, avec un flag `structureIds` pour savoir quand reconstruire les calques vs juste retraiter. `liveIds` inclut `playDir` ; `structureIds` non (le sens de lecture ne touche pas les calques). Fin du script : `syncLabels()` → `updatePlayBtnState()` → `setLang(initialLang())`.

## État actuel

Fonctionnel, testé par l'utilisateur, exporte bien vers Logic Pro. Publié : https://github.com/neoyume/dessons

Licence **PolyForm Noncommercial 1.0.0** (choix explicite de l'utilisateur, après CC BY-NC 4.0) : usage/modif/partage réservés aux fins non commerciales, interdit de vendre ou redistribuer l'outil (modifié ou non) comme produit/service payant, ou de l'intégrer à un tel produit. En revanche la **musique produite avec Dessons n'est pas couverte** — l'utilisateur peut la vendre. Le fichier `LICENSE` a un en-tête qui clarifie ce point, suivi du texte légal PolyForm complet. Ne pas repasser en MIT ni en CC sans demande explicite. Le footer de l'app (`footerFree`) dit explicitement "source-available" (pas "open source" — faux depuis le passage à PolyForm, une licence non-OSI) et rappelle que le MIDI produit appartient à 100 % à l'utilisateur quel que soit l'usage.

Docs bilingues : `README.md` (anglais, fichier principal vu par défaut sur GitHub) + `README.fr.md` (français), liés en tête l'un de l'autre. `CLAUDE.md` reste en français.

Ajouts successifs (tous rétro-compatibles avec l'export `.mid` de base) :
- **Web MIDI temps réel** (bouton « Jouer dans Logic ») : boucle en direct vers un port virtuel, un canal par calque, planification juste-à-temps (les réglages s'entendent immédiatement). Chrome/Edge uniquement. `#midiPort` (le `<select>` de port) est `hidden` depuis que l'UI a été simplifiée : `refreshPorts()` auto-sélectionne déjà un port (`/IAC|bus/i` préféré, sinon le premier) dès que `ensureMidi()` résout, ce qui arrive au clic sur "Play in DAW" — le sélecteur manuel était redondant pour le cas courant (un seul port virtuel). Gardé dans le DOM (pas supprimé) parce que `refreshPorts()`/`midiPortSel` le lisent et l'écrivent toujours comme source de vérité ; si un jour il faut un vrai choix multi-port, il suffit de retirer `hidden`.
- **Sustain (aperçu & MIDI)** (`#sustainToggle`, dans la section MIDI de `.panel-source`, juste au-dessus de l'accordéon « Comment connecter à Logic » — reste à cet endroit même si son effet dépasse maintenant le seul MIDI, le libellé le précise) : deux mécanismes bien distincts selon la sortie, ajoutés en deux temps (Web MIDI d'abord, sur demande explicite scopée à ce seul cas ; le synthé d'aperçu ensuite, l'utilisateur ayant testé et remarqué qu'il n'entendait aucune différence sans DAW). **Web MIDI** : envoie un vrai CC64 (pédale de sustain standard MIDI) — c'est Logic (ou tout instrument recevant le port virtuel) qui interprète le CC64 avec **son propre** moteur de sustain, Dessons ne simule rien côté audio, il relaie juste le contrôleur. `startPlayback('midi')` envoie CC64=127 sur chaque canal de `usedChannels` si la case est cochée au moment du clic sur "Play in DAW" ; un listener `change` dédié permet de l'actionner **en direct pendant la lecture** (équivalent d'appuyer/relâcher une vraie pédale). `stopPlayback()` envoie **toujours** CC64=0 sur `usedChannels`, indépendamment de l'état de la case — filet de sécurité pour ne jamais laisser une pédale engagée sur l'instrument recevant le port après l'arrêt de Dessons, ajouté à côté du nettoyage déjà existant (note-off explicites de `hangingNotes` + CC 120/123). **Synthé d'aperçu** : `scheduler()` calcule déjà `gateMs` (durée de note raccourcie par `Gate%`) pour caler `spawnVoice()` ; avec sustain coché et `sink==='synth'`, `synthDurMs` vaut `spanMs-3` (la pleine durée du pas) au lieu de `gateMs` — la note sonne sur tout son créneau plutôt que d'être coupée court par le Gate%. Ça seul s'est révélé **trop subtil pour s'entendre** (retour direct de l'utilisateur après test) : la plupart des timbres de `timbreFor()` ont un `sus` bas voire nul (les percussions/mallets type Vibraphone/Steel drums retombent quasi au silence pendant leur tenue) et un `rel` court, donc allonger juste la durée "note on" ne change presque rien à ce qu'on entend. Fix dans `spawnVoice(..., sustain)` (6ᵉ paramètre, `sustainOn` passé par `scheduler()`) : quand `sustain` est vrai, `rel = tb.rel*4` et `sus = Math.max(tb.sus, 0.55)` — un Vibraphone qui retombait au silence en 0.4s tient maintenant à 55% du volume et ne s'éteint qu'après 1.6s, un vrai effet de pédale de damper relevée plutôt qu'un simple prolongement de quelques dizaines de ms. `voices` (le tracking pour le plafond de 96 voix) n'a rien à changer : le nettoyage est piloté par l'event natif `osc.onended`, une release plus longue fait juste vivre l'entrée plus longtemps dans le `Set`, pas de fuite. Volontairement **pas** un vrai "tenu jusqu'au pedal-up" (qui empilerait des voix indéfiniment dans une boucle qui tourne en continu, jusqu'à un mur de son) — un compromis "durée+niveau+release boostés mais toujours bornés" plus sûr, sans changer le nombre de voix actives ni leur date de déclenchement. Le note-off réel envoyé en MIDI (`sink==='midi'`, `at + gateMs`) n'est **jamais** affecté par ce toggle : c'est le CC64 séparé qui gère la tenue côté DAW, pas le timing du note-off lui-même. Pas dans `PRESET_KEYS`/`CONTROL_DEFAULTS` : traité comme mute/solo, un réglage de performance éphémère plutôt qu'un paramètre de génération de la boucle.
- **Auto-play du synthé d'aperçu au chargement d'un média** (`autoStartPreviewOnLoad()`) : dès qu'une image/vidéo/webcam finit de charger (fin de `image.onload`, du `loadeddata` vidéo, du `ready()` webcam dans `loadFile()`/`toggleWebcam()`), la boucle démarre toute seule dans le synthé du navigateur — demande explicite de l'utilisateur pour ne plus avoir à cliquer « Écouter l'aperçu » à chaque fois. Réutilise `startPreviewPlayback()`, extrait du click handler de `#previewBtn` (même code exact, pas de logique dupliquée). Ne déclenche **jamais** si quelque chose joue déjà (`if(playing) return`) — n'interrompt pas un aperçu déjà lancé. Piège trouvé et corrigé : `loadFile()`/`toggleWebcam()` appellent déjà `stopPlayback()` sans condition en tout début de fonction avant de charger le nouveau média — donc `playing` est toujours redevenu `false` au moment où `autoStartPreviewOnLoad()` s'exécute, **même si une session Web MIDI réelle tournait vers Logic** juste avant. Sans garde-fou, déposer une nouvelle image pendant un jam MIDI aurait fait basculer silencieusement la sortie vers les haut-parleurs du navigateur. Fix : chaque fonction capture `wasPlayingMidi = playing && sink==='midi'` **avant** son propre `stopPlayback()`, et le passe en paramètre à `autoStartPreviewOnLoad(wasPlayingMidi)` — si la session coupée était du MIDI réel, l'auto-play est sauté, la lecture reste simplement arrêtée (comportement d'avant cette feature), sans tentative de reprendre le MIDI automatiquement (hors scope de la demande). Fiabilité par plateforme : repose sur le geste de chargement (clic Image/Vidéo/Webcam, ou un `drop`) qui compte encore comme activation utilisateur « récente » au moment où le média finit de charger — vrai en pratique pour un fichier local (quasi instantané) mais moins garanti que le clic direct sur `#previewBtn` ; la webcam est le cas le plus fragile (la popup de permission peut faire perdre cette fenêtre), et iOS Safari reste la plateforme la plus stricte. Si l'auto-play ne s'entend pas, `#synthState` (déjà l'outil de diagnostic existant) montre `ac.state` — même réflexe que d'habitude avant de deviner.
- **Sens de lecture** (gauche→droite, droite→gauche, haut→bas, bas→haut) : aperçu + temps réel + export.
- **Vidéo & webcam** en source, ré-analysées en live (~15 fps) → la boucle MIDI suit l'image. Bouton « Figer ».
- **Synthé d'aperçu Web Audio** (bouton « Écouter l'aperçu ») : joue la boucle dans le navigateur sans DAW, sons de synthé approximatifs mappés sur les familles GM. Zéro dépendance. Marche aussi sur Safari (contrairement au Web MIDI).
- **Mute / Solo par calque** (boutons M/S) : filtre la lecture temps réel (aperçu + Web MIDI), pas l'export.
- **Courbe de lecture** (`curve`, 6 presets dont `custom`) : time-warp de la tête de lecture (accéléré / ralenti / pulsé / swing / personnalisée via un mini éditeur à 3 poignées). Affecte l'aperçu, le Web MIDI **et** l'export.
- **Saturation** (`sat`, 0–200%) : à côté du contraste, même traitement (per-pixel dans `process()`, `filter` CSS dans l'aperçu).
- **Séparation « par couleurs » avec pipette** (`sepMode='pick'`) : clique la pastille d'un calque → armé → clique l'image → couleur échantillonnée devient la cible du calque, **isolée** (tolérance réglable, le reste de l'image est silencieux pour ce calque, pas réassigné ailleurs).
- **Interface bilingue FR/EN** avec détection auto + mémorisation.
- **Don / Ko-fi** : `ko-fi.com/neoyume`. Lien dans le `<footer>` de l'appli (i18n : `footerFree` / `footerKofi`), badge + section « Soutenir » dans les README, `.github/FUNDING.yml` pour le bouton Sponsor GitHub. Pas de fonctionnalité payante — le don est purement optionnel, ne jamais gater quoi que ce soit derrière.
- **Enrichissement de l'analyse d'image** (voir le pipeline détaillé plus haut) : lissage/flou pré-seuillage (`#blur`), seuil automatique par méthode d'Otsu (bouton `#autoThresh`), courbe de tons à 3 points remplaçant le contraste unique (`#blackPt`/`#gamma`/`#whitePt`), inversion (`#invert`), une 4ᵉ méthode de séparation « Par contours (Sobel) » (`sepMode='sobel'`) qui bucket par orientation d'arête plutôt que par teinte, et un recadrage (zone d'intérêt) glisser-déposer sur l'aperçu (`#cropBtn`, `cropRect`) appliqué en amont de tout le reste du pipeline. (La postérisation de teinte qui faisait aussi partie de ce lot a depuis été retirée, voir plus bas.)
- **Contrôles visuels remplaçant les nombres bruts** (voir le détail de chacun plus haut) : piano roll (`#pianoRoll`) rejouant `lastNotes` à la place du compteur « X notes », masque de sensibilité en direct sur l'aperçu (`#sensMask`), courbe de tons remplacée par un graphe interactif avec histogramme (`#toneCurveGraph`, calcule toujours via `buildToneLUT()`), une animation de balayage au survol de Direction/Courbe (`#scanOverlay`) qui démontre le sens et le tempo réels sans lancer la lecture, un mini clavier sous Gamme/Note fondamentale (`#scaleKeyboard`) mettant en évidence les degrés de la gamme et la fondamentale, et des pastilles de couleur détectée par calque (`.swatchDetected`, `#layerSwatchStrip`) montrant la vraie couleur moyenne de chaque calque à côté de sa couleur d'identité synthétique.

## Pistes d'itération possibles (non demandées explicitement, à proposer si pertinent)

- Mode "grille régulière" en alternative au mode contours, pour forcer des motifs rythmiques plus prévisibles/carrés plutôt que suivre la forme de l'image
- ~~Prévisualisation audio dans le navigateur~~ → **fait** (synthé Web Audio, bouton « Écouter l'aperçu »). Amélioration possible : vrais échantillons GM (soundfont) si les sons de synthé actuels sont jugés trop pauvres — mais ça coûte le zéro-dépendance ou des Mo de samples à fetch
- ~~Presets~~ → **fait** (`#presetBar`, `BUILTIN_PRESETS` + `localStorage['dessons.presets']`, voir plus haut). Reste possible : undo/historique des réglages, hors scope des presets.
- ~~Détection de contours plus fine (vrai Sobel)~~ → **fait** (`sepMode='sobel'`, bucket par orientation d'arête). Reste possible : Canny (hystérésis + suppression non-maximale) si Sobel s'avère encore trop bruité sur certaines images.
- Export vers d'autres formats (Ableton .als, simple CSV de notes, ou JSON réutilisable)
- Mode batch : traiter un dossier d'images d'un coup pour générer plusieurs loops
- Choix de la caméra (`enumerateDevices`) et résolution webcam ; enregistrement de la sortie vidéo webcam avec la boucle

## Ce qu'il faut éviter de casser

- Le zéro-dépendance / zéro-build : ne pas introduire npm/webpack sans que ce soit une décision explicite et assumée avec l'utilisateur
- Le fait que ça reste utilisable en `file://` direct (double-clic) en plus de GitHub Pages
- La logique de gamme (aucune note hors gamme ne doit pouvoir sortir) — c'est un principe central du projet, pas un détail
- Le fait que l'export `.mid` marche sans Web MIDI : le mode temps réel est un bonus, jamais un prérequis. Si `navigator.requestMIDIAccess` est absent, le reste doit fonctionner normalement (seul le bouton « Jouer dans Logic » est désactivé). Idem webcam : si `getUserMedia` manque, image + vidéo doivent marcher.
- Pas de notes bloquées : tout ajout au scheduler temps réel doit garantir un note-off correspondant, et `stopPlayback()` doit rester le filet de sécurité (clear + note-off explicites + CC 123).
- La parité FR/EN : toute nouvelle chaîne visible passe par `I18N` (les deux langues) ou `[data-i18n]`, jamais en dur. `t()` pour le JS. Vérifier que `Object.keys(I18N.fr)` == `Object.keys(I18N.en)`.
- Le nettoyage des sources : toujours passer par `stopSource()` avant d'en démarrer une autre (sinon caméra qui reste allumée, timers qui s'empilent).
